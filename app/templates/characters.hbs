<h1>Characters Page</h1>

{{#each this.model as |record|}}
   <LinkTo @route='character' @model={{record}}><p>Character name: {{record.aliases}}</p></LinkTo>
    <p>Culture: {{record.culture}}</p>
{{/each}}